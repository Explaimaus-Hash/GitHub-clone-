{{template "base/head" .}}
<div class="repository view issue pull commits cf-issues cf-pull-commits">
	{{template "repo/header" .}}
	<div class="ui container">
		<div class="cf-issues__toolbar navbar">
			{{template "repo/issue/navbar" .}}
			<div class="ui right">
				<a class="cf-btn cf-btn--primary ui green button {{if not .PullRequestCtx.Allowed}}disabled{{end}}" href="{{.RepoLink}}/compare/{{.BranchName}}...{{.PullRequestCtx.HeadInfo}}">{{.i18n.Tr "repo.pulls.new"}}</a>
			</div>
		</div>
		<div class="cf-divider ui divider"></div>
		{{template "repo/issue/view_title" .}}
		{{template "repo/pulls/tab_menu" .}}
		<div class="ui bottom attached tab pull segment active">
			{{template "repo/commits_table" .}}
		</div>
	</div>
</div>
{{template "base/footer" .}}
