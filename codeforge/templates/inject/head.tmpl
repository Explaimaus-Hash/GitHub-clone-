{{/*
  CodeForge — head injection.
  Intentionally empty: codeforge.css and fonts are loaded directly by
  custom/templates/base/head.tmpl. This file overrides the embedded
  inject/head.tmpl (which would otherwise be a no-op) and the legacy
  stitch-theme overlay.
*/}}
