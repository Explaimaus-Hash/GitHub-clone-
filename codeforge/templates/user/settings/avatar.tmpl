{{template "base/head" .}}
<main class="cf-settings user settings avatar">
	<div class="ui container">
		<div class="ui grid">
			{{template "user/settings/navbar" .}}
			<section class="cf-settings__content twelve wide column content">
				{{template "base/alert" .}}
				<h4 class="cf-settings__header ui top attached header">
					{{.i18n.Tr "settings.avatar"}}
				</h4>
				<div class="cf-settings__segment ui attached segment">
					<form class="cf-form ui form" action="{{.Link}}" method="post" enctype="multipart/form-data">
						{{.CSRFTokenHTML}}
						{{if not DisableGravatar}}
						<div class="inline field">
							<div class="ui radio checkbox">
								<input name="source" value="lookup" type="radio" {{if not .LoggedUser.UseCustomAvatar}}checked{{end}}>
								<label>{{.i18n.Tr "settings.lookup_avatar_by_mail"}}</label>
							</div>
						</div>
						<div class="inline box field {{if .Err_Gravatar}}error{{end}}">
							<label for="gravatar">Avatar {{.i18n.Tr "email"}}</label>
							<input class="cf-input" id="gravatar" name="gravatar" value="{{.LoggedUser.AvatarEmail}}" />
						</div>
						{{end}}

						<div class="inline field">
							<div class="ui radio checkbox">
								<input name="source" value="local" type="radio" {{if .LoggedUser.UseCustomAvatar}}checked{{end}}>
								<label>{{.i18n.Tr "settings.enable_custom_avatar"}}</label>
							</div>
						</div>

						<div class="inline box field">
							<label for="avatar">{{.i18n.Tr "settings.choose_new_avatar"}}</label>
							<input class="cf-input" name="avatar" type="file" >
						</div>

						<div class="cf-form__actions field">
							<button class="cf-btn cf-btn--primary ui green button">{{$.i18n.Tr "settings.update_avatar"}}</button>
							<a class="cf-btn cf-btn--danger ui red button delete-post" data-request-url="{{.Link}}/delete" data-done-url="{{.Link}}">{{$.i18n.Tr "settings.delete_current_avatar"}}</a>
						</div>
					</form>
				</div>
			</section>
		</div>
	</div>
</main>
{{template "base/footer" .}}
